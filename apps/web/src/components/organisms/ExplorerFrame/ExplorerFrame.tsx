import type { PropsWithChildren } from 'react';
import { AppShell } from '../../atoms/AppShell/AppShell';

export const ExplorerFrame = ({ children }: PropsWithChildren) => <AppShell>{children}</AppShell>;
